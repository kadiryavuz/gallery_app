import React from 'react';

/**
 * stateless Loading indicator to be used where needed
 */
const Loading = () => {

    return (
        <div>
            <h3>
                Loading...
         </h3>
        </div>
    );
}

export default Loading;